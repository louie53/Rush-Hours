{
  "name": "rush-hours-root",
  "private": true,
  "scripts": {
    "postinstall": "npm i --prefix client && npm i --prefix server",
    "lint": "npm run lint --prefix client && npm run lint --prefix server"
  }
}
